{% extends "formularios/base.html" %}

{% block stylesheets %}
<style>
table {
  font-size: 90%;
  table-layout: fixed;
}
table th, table td {
  overflow: hidden;
}
</style>
{% endblock stylesheets %}

{% block title %}Formularios publicados{% endblock %}

{% block tab_publicados %}active{% endblock %}

{% block formularios_body %}
<div class="row">
  <div class="span8">
    <div class="row">
      <div class="span5">

{% include "search.html" %}

      </div>
      <div class="span3">

{% include "pagination.html" with current_page=page %}

      </div>
    </div>

    <div class="row">
      <div class="span8">
	<table class="table table-hover table-condensed">
	  <caption>
{% if formularios %}
Mostrando elementos {{ first }}-{{ last }}.
{% else %}
No hay formularios publicados.
{% endif %}
	  </caption>
	  <thead>
	    <tr>
	      <th style="width: 5%;">#</th>
	      <th style="width: 23%;">Nombre</th>
	      <th style="width: 13%;">Autor</th>
	      <th style="width: 25%;">Palabras clave</th>
	      <th style="width: 12%;">Fecha</th>
	      <th style="width: 22%;"></th>
	    </tr>
	  </thead>
	  <tbody>
{% for f in formularios %}
{% cycle '' 'background-color: #F9F9F9' as rowstyles silent %}
<tr style="{{ rowstyles }}">
  <td>{{ forloop.counter0|add:first }}</td>
  <td title="{{ f.descripcion }}">{{ f.descripcion }}</td>
  <td title="{{ f.autor }}">{{ f.autor }}</td>
  <td title="{{ f.METADATA }}">{{ f.METADATA }}</td>
  <td title="{{ f.fecha_creacion|date:"d/m/Y" }}">{{ f.fecha_creacion|date:"d/m/Y" }}</td>
  <td>
    <button class="btn btn-mini btn-info" type="button">+info</button>
    <button class="btn btn-mini btn-info" type="button">ver</button>
    <button class="btn btn-mini btn-primary" type="button">usar</button>
  </td>
</tr>
{% endfor %}
	  </tbody>
	</table>
      </div>
    </div>
  </div>
  <div class="span4">
    <div style="border-left: 1px solid #dddddd; padding-left: 12px;">

{% include "formularios/evaluacion_form.html" %}

    </div>
  </div>
</div>

{% endblock formularios_body %}
